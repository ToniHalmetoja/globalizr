import { useLocation } from "react-router";
import { useNavigate } from "react-router";
import { Container, Button } from "react-bootstrap";

export const Login = () => {

    return (
        <Container>
            <h1>GlobalizR</h1>
            <h2>Your caleidoscope of experiences</h2>
            <Button>Log into GLOBALIZR!</Button>
        </Container>

    )
}